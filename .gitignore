# dependencies
node_modules/

# build output
dist/

# env & secrets
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*

# os / editor
.DS_Store

# internal smoke harness — local-only, talks to the dev DB, not shipped
scripts/smoke.mjs

# claude code — personal/local settings only.
# Shared tooling (.claude/commands/, .claude/settings.json) stays tracked on purpose.
.claude/settings.local.json

