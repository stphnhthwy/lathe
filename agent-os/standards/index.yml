# Agent OS Standards Index

global:
  commits:
    description: Commit message conventions — type-prefixed, present tense, concise subjects
  publishing:
    description: How to publish @lathe/cli to npm — one-time org/2FA setup, publish flow, version bumps, prereleases, and unpublish/deprecate rules

testing:
  tdd:
    description: Test-driven development workflow and the vitest build-gate rule
  packaging:
    description: Four layers of testing a published package — library, CLI, packaging (pack/publint/attw), and consumer install smoke test

manifest:
  spec-not-code:
    description: The capability manifest is declarative and never executes; the declare/defer dial and frozen locked compute

cli:
  commands:
    description: How a CLI subcommand is structured — one file per command, logic in libraries, readonly vs confirm semantics
